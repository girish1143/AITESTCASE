PORT=5000

# Optional: Add a GitHub Personal Access Token (PAT) to bypass the 60 requests/hour unauthenticated API rate limit.
# Create a token at https://github.com/settings/tokens (classic) with no scopes required for public repo access.
GITHUB_TOKEN=
